import React, { useEffect, useState } from "react";
import Head from 'next/head'
import styles from '../styles/Home.module.css'
import Header from '../component/Header'
import Footer from "../component/Footer"
import Lot from "../component/Lot"
import LoadingScreen from "../component/LoadingScreen"
import tea1 from "../image/tea1.png"
import tea2 from "../image/tea2.png"
import tea3 from "../image/tea3.png"
import tea4 from "../image/tea4.png"
import Link from "next/link"


export default function Resultat() {
  const [lot, setLot] = useState(0);
  const [loading, setLoading] = React.useState(true);
   
  useEffect(() => {
    makeLot(4)
  }, []);

     const makeLot = (max) =>{
       const x = Math.floor(Math.random() * max);
       setLot(x)
     }

     React.useEffect(() =>{
      setTimeout(() => setLoading(false), 2000);
     })



  return (
    <>

{!loading ? (
        <React.Fragment>
            <div className={styles.elm} style={{backgroundColor:"#41D8C2"}}>
      <Head>
        <title>TeaBingo - Jeux concours</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/fav.png" />
      </Head>
      <Header/>
      <section className={styles.block} style={{borderBottom:"solid 1px white"}}>
        <h1 className={styles.h1} style={{color:"white"}}>Félicitation !</h1>
        <p style={{fontSize:20,color:"white"}}>Vous avez gagné </p>
         { lot === 0 && <Lot name={"Un infuseur à thé"} image={tea1}/> }
         { lot === 1 &&  <Lot name={"Une boite de 100g d’un thé détox ou d’infusion"} image={tea2}/> }
         { lot === 2 &&   <Lot name={"Une boite de 100g d’un thé signature"} image={tea3}/> }
         { lot === 3 &&  <Lot name={"Un coffret découverte d’une valeur de 39€"} image={tea4}/> }
         { lot === 4 &&   <Lot name={"Un coffret découverte d’une valeur de 69€"} image={tea4}/> }
        <Link  href="/tickets">
          <button className={styles.action2} style={{color:"#41D8C2"}}>Voir tout mes tickets</button>
        </Link>
        <small style={{color:"white"}}> Lot a récupérer en magasin avant le 31 / 04 /2022</small>
        <div style={{display:"flex",
        flexDirection:"column",
        justifyContent:"center",
        alignItems:"center",
        padding:15,
        borderTop:"solid 1px white",
        margin:30 }}>
         <p style={{fontSize:20,color:"white"}}> Vous êtes également été selectionné pour le grand tirage au sort</p>
         <Link  href="/jeux">
         <button className={styles.action2} style={{color:"#41D8C2",maxWidth:250}}>En savoir plus </button>
         </Link>
      </div>
      </section>

      <Footer/>
    </div>
        </React.Fragment>
      ) : (
        <LoadingScreen />
      )}
    </>
  )
}
